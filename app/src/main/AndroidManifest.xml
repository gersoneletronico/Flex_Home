<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.g_automation.flex_home">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity_Principal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Config_Modulos"
            android:label="Configurar Acionamento Grupos"></activity>

        <activity
            android:name=".Confugurar_btns"
            android:label="@string/title_activity_confugurar_btns"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".Configurar_servidores"
            android:label="@string/title_activity_confugurar_servidor"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".Lista_de_ips"
            android:label="IPs"></activity>
        <activity android:name=".Lista_de_comandos"
            android:label="Configurar Módulos"></activity>
        <activity android:name=".Activity_Config_Voz"
            android:label="Configurar Comando de Voz"></activity>
        <activity android:name=".Activity_Config_Grupos"
            android:label="Configurar Acionamento Grupos"></activity>
        <activity android:name=".Activity_Config_Acio_Grupos"
            android:label="Configurar Acionamento Grupos"></activity>
    </application>

</manifest>